server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/saurus
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create  # create / update / validate / none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  redis:
    host: localhost
    port: 8080

  security:
    user:
      name: admin
      password: admin1234  # 테스트용으로만 사용

jwt:
  secret:
    key: bXktdmVyeS1zZWNyZXQta2V5LWZvci1qd3QtMTIzNDU2Nzg5MA==
  expiration: 3600000  # 1시간 (ms)

logging:
  level:
    org.hibernate.SQL: debug
    org.springframework.security: debug
